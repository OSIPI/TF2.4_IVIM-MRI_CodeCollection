[pytest]
# Scope collection to the NPE scaffold so pytest's rootdir pins here and the
# repo-wide OSIPI suite (which needs the other, incompatible venv) is not pulled in.
testpaths = tests
pythonpath = .
addopts = -q
